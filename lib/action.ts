import { GraphQLClient } from "graphql-request";


const isProduction = process.env.NODE_ENV ==='production'

const makeGraphQLRequest = async (quary: string,
    variables = {}) => {

    try {

    } catch (err) {
        throw err
    }
}